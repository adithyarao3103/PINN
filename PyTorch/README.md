# PyTorch implementation of PINNs

In this folder, the implementation of PINNs using the PyTorch library is presented.

This implementation was done later, as I first started off with Tensorflow, and therefore do not have all the notebooks that are present in the Tensorflow implementation, especially in the two variables.
